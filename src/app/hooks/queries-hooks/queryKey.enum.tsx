/* eslint-disable no-unused-vars */
export enum QueryKeys {
  ApprovedVideos = 'approvedVideos',
  TrendingVideoIds = 'trendingVideoIds',
  MetaCategories = 'meta-categories',
  MetaSpeakerNames = 'meta-speakerNames',
  MetaTags = 'meta-tags',
  Meta = 'meta',
}
