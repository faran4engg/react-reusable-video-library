// faran - start
// this setup in the interface for Route component
// import { RouteComponentProps } from 'react-router-dom';

// type RouteParams = {
//   id: string; // must be type string since route params
// };

// export interface WatchPageProps extends RouteComponentProps<RouteParams> {}

// faran - end

export interface WatchPageProps {}
